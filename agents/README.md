# Agent Repositories

This directory is a lightweight pointer for the thirty separate experiment repositories.

Do not implement experiments in this repository. Each experiment should live in its own `agent-XX-name` repository and link back here through the programme records.

The canonical programme-level index is [`registers/agent-index.md`](../registers/agent-index.md). Update that register when an experiment repository is created, evaluated, published or archived.
